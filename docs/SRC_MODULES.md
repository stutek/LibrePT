---
type: index
title: LibrePT Source Module Catalog
description: Catalog of every runtime module under src/ — the app entry, data layer, feature modules, controllers, and service worker.
status: active
tags:
  - index
  - src
  - okf
---

# LibrePT Source Module Catalog

The runtime app lives under `src/` (served as the web root locally and flattened into `dist/`
on deploy). It's a native ES-module app (`<script type="module" src="app.js">`). `src/app.js`
is structured into feature modules under `src/modules/` (`session`, `plans`, `clients`, `exercises`, `history`, `common`, `themes`), the training vocabulary under `src/domain/`, and storage under `src/data/`.

**The tree is layered, and the layering is gated** by
[agent_tools/import_layers.py](../agent_tools/import_layers.py) — each layer may import only from
those strictly below it: `data/` → `domain/` → `modules/common/` → `modules/<feature>/` →
`controllers/` → `app.js`. The two lowest are the ones easily confused: **`data/` is about records
at rest** (their shape, identity, ordering, persistence), while **`domain/` is the training
vocabulary** — what a modality is, how reps and load are authored, what a session's clock means —
pure, with no DOM and no storage. `domain/` was carved out of `modules/common/` in
[TODO §24.6](../TODO.md), which had grown into two directories wearing one name: a
DOM-reference count split its modules cleanly into ten with zero and the rest with 5–35, so a
directory documented as "shared UI helpers" was also holding half the domain rules.

**`src/index.html` and `src/index.css` are shells, not feature owners.** `index.html` holds only
`<head>` boilerplate, the boot-critical integrity-error overlay, and empty named canvases
(`#app-header`, `#main-content`, `#notification-area`, `#active-session-overlay`,
`#clipboard-timer-stack`, `#dialogs-root`); every view section, dialog, the header, and the
notification area render their own markup from the JS module that owns their behavior (e.g.
`renderHeaderShell()` in `applicationHeader.js`, `renderClientDialog()` in `formsController.js`).
`index.css` holds only shared design-system tokens/foundations (buttons, cards, modals, form
controls, color tokens) — each feature's own rules live in a same-named `.css` file next to its
module, listed in the table below alongside that module's `.js`.

| Module | Type | Description |
| :--- | :--- | :--- |
| [src/app.js](../src/app.js) | `entry` | Application bootstrapper: root initialization, dependency injection wiring, and global lifecycle hooks. |
| [src/appBoot.js](../src/appBoot.js) | `entry` | Side-effect-free boot steps extracted from `app.js`'s `init()` — one exported `bootXyz(deps)` per feature, callable independently of the real boot (used by `tests/medium/` to mount one component without running the full app). |
| [src/index.html](../src/index.html) | `shell` | The app shell: `<head>`, the integrity-error overlay, and empty canvases every other module renders into. |
| [src/index.css](../src/index.css) | `styles` | Shared design-system tokens and foundations only (buttons, cards, modals, form controls, resets) — per-feature rules live in each module's own `.css`. |
| [src/landing.html](../src/landing.html) | `generated` | **Generated — do not edit.** The trainer-facing front door ([docs/LANDING.md](LANDING.md)), rendered by [render_docs.py](../agent_tools/render_docs.py) and gated for drift. README.md is developer-facing and correctly stays that way; a trainer arriving from a forum post needs one screen — what it is, a demo they can drive themselves, and how to keep it on their phone. |
| [src/privacy.html](../src/privacy.html) | `generated` | **Generated — do not edit.** The privacy policy as a shipped page, rendered from the canonical [PRIVACY.md](../PRIVACY.md) by [agent_tools/render_docs.py](../agent_tools/render_docs.py) and gated for drift in Stage 1. Exists so the policy is reachable offline and lives on a domain we can prove we own (OAuth verification requires the latter). |
| [src/preview.html](../src/preview.html) | `generated` | **Generated — do not edit.** The PREVIEW data-loss notice ([docs/PREVIEW.md](PREVIEW.md)), linked from the header PREVIEW tag. Shipped in-app because the one warning telling a trainer their data can vanish was itself unreachable without signal. |
| [src/bug-reporting.html](../src/bug-reporting.html) | `generated` | **Generated — do not edit.** The bug-reporting guide ([docs/BUG_REPORTING.md](BUG_REPORTING.md)), linked from the app menu. The *guide* is now readable without a GitHub account; the issue tracker it points to still needs one. |
| [src/consent-form-en.html](../src/consent-form-en.html), [src/consent-form-sl.html](../src/consent-form-sl.html) | `generated` | **Generated — do not edit.** The client consent form per language ([docs/templates/](templates/INDEX.md)). Handed to a **gym client**, so the language is the client's (`gdprConsent.formLang`), not the app's. |
| [src/privacy-notice-en.html](../src/privacy-notice-en.html), [src/privacy-notice-sl.html](../src/privacy-notice-sl.html) | `generated` | **Generated — do not edit.** The Art. 13 client privacy notice per language. Its URL is interpolated into the consent email, so [consentForm.js](../src/modules/common/consentForm.js) builds an **absolute** URL from `import.meta.url` — a relative one is meaningless once it leaves the app. |
| [src/docs.css](../src/docs.css) | `styles` | The only stylesheet for the generated documentation pages. Standalone rather than reusing `index.css`, whose five themes are selected by `theme-boot.js` at runtime — these pages load no scripts at all, so light/dark comes from `prefers-color-scheme`. |
| [src/theme-boot.js](../src/theme-boot.js) | `entry` | Render-blocking classic script that sets the theme class before paint (anti-FOUC) and forces http→https; external so CSP `script-src` can forbid `'unsafe-inline'`. |
| [src/data/stateStore.js](../src/data/stateStore.js) | `data` | Central app state management (TODO §18.6 part 4): the in-memory state object stays synchronous, but load-at-boot and save-on-write now run through IndexedDB via a one-time, revertable import from the legacy `localStorage` bucket — falls back to plain `localStorage` if IndexedDB is unavailable. Also owns the move between workspaces (TODO §40.3): `switchWorkspace` drains the write queue before it closes the connection, or an enqueued write of the workspace being left would flush against the one being entered, and `resetSandbox` deletes a database that cannot be the trainer's. |
| [src/data/storageNamespace.js](../src/data/storageNamespace.js) | `data` | The app's plain `localStorage` keys (TODO §16.5/§16.3, both resolved): no bucket-keying scheme here any more — the schema axis lives in IndexedDB's per-schema stores, and `librept_db` is now only the one-time legacy import source / no-IndexedDB fallback. **The one axis that does live here is the WORKSPACE** (TODO §40.1): the version-scoped accessors suffix their key with the active workspace, so a fact about the DATA is per workspace while a fact about the PERSON — theme, language, accepted terms — is shared by both and read unscoped. |
| [src/data/workspace.js](../src/data/workspace.js) | `data` | Which of the two databases the app is looking at (TODO §40): the trainer's own work, or the sandbox they learn and experiment in. Turns that one name into a database name and a `localStorage` key suffix, and nothing else. **The working workspace keeps today's exact names** — `librept`, unsuffixed keys — so arriving on this build moves nobody's data and a sandbox nobody has entered does not exist. A separate DATABASE rather than a store-name prefix, because a handle pointing elsewhere CANNOT write real records, where a guard inside the star write would only forbid it. |
| [src/data/displayPrefs.js](../src/data/displayPrefs.js) | `data` | How much of a card the trainer wants to see, kept between visits (TODO §42.4): ONE setting, for the day's session cards. Beside the theme in plain unscoped `localStorage`, shared by both workspaces, because it is a fact about the person. Defaults OFF: a preference that changes the app's look before anybody asks is a surprise, not a default. **The clipboard had a second setting and §42.14 removed it** — once every deck card became one design that says everything it has on its own row (§42.3), "open them all" had nothing left to open. Session cards genuinely hide participants and programme, so theirs stays.
| [src/data/lastRoute.js](../src/data/lastRoute.js) | `data` | Where the trainer was in the workspace they left (TODO §40.3), one path each, so coming back returns to the view rather than to the dashboard — the live session, the client and the exercise in focus included. Per workspace, and version-scoped like the live session cache beside it, so a sandbox reset drops it with everything else that sandbox held. Stores a path and returns one: whether it still names anything is the router's question, asked by the caller. |
| [src/data/sandboxStaleness.js](../src/data/sandboxStaleness.js) | `data` | Whether the sandbox has gone flat, and whether the trainer may be asked about it (TODO §40.4). Pure. Twelve hours, not the week the seed spans: the seeded board's live and upcoming session are what the demo IS, and they are behind the trainer by the next morning. A declined offer waits three hours, because the offer destroys what they did in there. An unknown seeding time is never stale — offering to delete somebody's work on no evidence is the expensive way to be wrong. |
| [src/data/indexedDb.js](../src/data/indexedDb.js) | `data` | IndexedDB adapter (TODO §18.6): one database with one object store per schema — the layout that lets a single transaction write every live schema atomically — plus promise wrappers that resolve on commit. |
| [src/data/storageDurability.js](../src/data/storageDurability.js) | `data` | Storage durability (TODO §18.6/§18.8): requests eviction-proof storage, and reports risk by measuring the consequence (quota, persistence) rather than sniffing for private browsing. |
| [src/data/backupHealth.js](../src/data/backupHealth.js) | `data` | "Is any of this anywhere it would survive losing this browser?" (TODO §3.8) — pure thresholds over a `{id, hash}` **fingerprint** of the last backup. A fingerprint rather than a snapshot because a feature warning about storage eviction must not double what the database holds; a downloaded file clears it exactly as a Drive sync does, which is what separates it from §3.9's Drive-relative ahead count. |
| [src/data/writeQueue.js](../src/data/writeQueue.js) | `data` | Write-behind persistence queue (TODO §18.6): serialises async writes behind a synchronous call so saves can never land out of order, and surfaces failures instead of swallowing them. |
| [src/data/schemaMigrations.js](../src/data/schemaMigrations.js) | `data` | **Legacy chain runner, awaiting deletion (TODO §16.5).** Chains are *not* the architecture: [TODO §18](../TODO.md) decided **star writes** — every record projected directly from the live domain object into each live schema, no step feeding another. This walks the old v1→v2→v3 chain and is kept only until the projections replace it. Do not extend it. |
| [src/data/migrationSteps.js](../src/data/migrationSteps.js) | `data` | `CURRENT_SCHEMA_VERSION` and the pure per-version transforms. The version constant survives star writes (it is what storage buckets key on, TODO §16.3); the ordered *chain* around it does not. |
| [src/data/clientErasure.js](../src/data/clientErasure.js) | `data` | An Art. 17 erasure as **anonymization, not deletion** (TODO §17.3): identity replaced with a pseudonym derived from the record's own opaque id, execution records kept. Irreversible by construction — no mapping is stored, because with no server it would live in the database it protects. Fans out to the `clientName` copies denormalised onto history/planUpdates, and refuses to rewrite free text shared with a same-named client. |
| [src/data/erasureSuppression.js](../src/data/erasureSuppression.js) | `data` | The register of erased ids (TODO §18.11), so restoring a pre-erasure backup cannot resurrect someone. Salted SHA-256 per entry, each with its own salt so two devices' registers can be **merged** rather than replaced; carried inside backup files and unioned on import, which is how it survives a reinstall. |
| [src/data/erasureRegisterSync.js](../src/data/erasureRegisterSync.js) | `data` | The register across devices and wipes: its own `appDataFolder` file (never a key inside the snapshot, which a restore would roll back), unioned on every sync pass with the local copy AND with entries re-derived from the erased records themselves. A grow-only set, so the merge is a union — no ancestor, no conflicts, and no pass can shrink it. |
| [src/data/erasureChecklist.js](../src/data/erasureChecklist.js) | `data` | What an erasure leaves for the trainer: the gym calendar (reachable on the existing Google grant — automatable once TODO §1.5 lands), sent mail and SMS (never reachable — composed in the trainer's own client), old backup files, and the one item to KEEP: the signed consent form. Plus the plain-text receipt. |
| [src/data/signupFile.js](../src/data/signupFile.js) | `data` | The submission as the artifact that travels (TODO §1.7): plain indented JSON, because it is a person's own data in transit and a format they cannot open would make it less trustworthy, not more. Declares **both** a media type (`application/vnd.librept.signup+json`) and a distinctive extension, since an Android intent routes on the type, an OS association on the extension, and email frequently relabels the type — the trailing `.json` is what keeps the file openable where no association exists (every desktop, and iOS). Filenames are sanitised hard: the client's name becomes a path on someone else's device. |
| [src/data/publicUrls.js](../src/data/publicUrls.js) | `data` | The two addresses this app has in the world — the deployed site and the issue tracker — declared once. They were spelled out in five places (the consent letter's links, the crash reporter, two pieces of header markup, the generated landing page), which is the shape of a value with no home: nothing is wrong until the repository moves, and then four of the five get updated. Absolute deliberately, because a relative link is meaningless the moment it leaves the app — in an email a client opens on their phone, or in a public issue. |
| [src/data/crashReport.js](../src/data/crashReport.js) | `data` | What a crash report contains, and what it must never contain (TODO §12.4). **The issue tracker is public, and that decides the design**: the payload is non-identifying BY CONSTRUCTION — a fixed list of safe fields (message, stack, route, build, time), with anything else a caller passes dropped, because a redaction pass that strips known-bad keys fails the first time someone adds a field nobody remembered to strip. The route travels since routes are durable and their ids are opaque (UC5), which makes the failing URL the best reproduction instruction there is. Nothing is ever sent: the link opens a prefilled issue the trainer reads on GitHub and submits themselves. Identical repeats collapse into one entry with a count, so a render loop cannot evict the report that explains how it started. |
| [src/data/feedbackRoute.js](../src/data/feedbackRoute.js) | `data` | Where a trainer's feedback goes (TODO §23.5) — an address that needs no account, because a GitHub issue is a wall to a personal trainer. Split by KIND, not preference: ideas and questions to the address, something BROKEN to an issue, since an email thread about a bug has no version, no page and no way for the next person who hits it to find it. What travels is four fixed lines (build, page, language, screen) — a fixed list rather than "everything we can find", the rule crashReport.js already follows. |
| [src/modules/common/feedbackRouteDialog.js](../src/modules/common/feedbackRouteDialog.js) | `component` | The dialog behind the ☰ menu's *Send feedback*. **The screenshot is asked for, not taken**: the only API that could photograph the screen opens a permission prompt, exists on desktop only, and on a phone would ask a trainer mid-session to grant screen capture to a web page — while the OS screenshot they already know is one gesture. Nothing is sent from here; both buttons open something the trainer reads and submits themselves, and the diagnostics are SHOWN before either is opened. |
| [src/data/inviteRecord.js](../src/data/inviteRecord.js) | `data` | The invitation, and with it the RSVP (TODO §1.6, decided 2026-08-17). **An RSVP is a fact about an invitation** — it was sent, and this came back — not a property of a person (who answers differently per session) nor of a session (which has no single answer). `sessions.participants` stays the authoritative attendee list, so an attendee added by hand simply has no invitation and that is ordinary rather than missing. **By reference only**: two ids and the facts about the message, no name or contact — erasing a client leaves every invitation structurally valid with nothing in it to redact. An answer arriving with no invitation on this device is still recorded, because the reply is itself evidence one was sent; it just claims no `sentAt` it never saw. |
| [src/data/clientSignup.js](../src/data/clientSignup.js) | `data` | What a prospective client's own introduction of themselves IS (TODO §1.7/§26) — identity, contact, and the consent stamp taken on **their** device. How it travels is deliberately elsewhere (§1.6's transport seam). Two decisions: **goals and injuries are the client's to offer** (ruled 2026-08-17), optional at every level and absent rather than blank when unsaid, and they may travel only inside the shared FILE — never a URL, where Art. 9 data would rest in a carrier's logs and two message histories; and **`formVersion`/`formLang` are captured when the client was shown that wording**, since a self-served consent that loses either evidences nothing under Art. 7(1). Reading one is parsing hostile input: field-by-field, unknown keys dropped, so nothing a sender invents reaches the register. |
| [src/data/clientConsent.js](../src/data/clientConsent.js) | `data` | The three states a stored `gdprConsent` can be in and the one transition between them (TODO §27.4/§27.7). **Never-consented and withdrawn are different situations**: processing stops for both, but only one is a client the trainer must still be able to prove once agreed (Art. 7(1)). `withdrawConsent` therefore clears `cloudSync` — the flag every caller already reads to decide whether it may act — while keeping the signed date, the wording version and the language. The old untick-the-box path wrote all three blank, so honouring a withdrawal destroyed the evidence of the consent it ended. Withdrawal is not erasure (§27.2): it halts processing and deletes nothing. |
| [src/data/clientDataExport.js](../src/data/clientDataExport.js) | `data` | One client's data for an Art. 15/20 request, as JSON and as readable Markdown. Whitelist-scoped so no other client's data can leak in — a group session is disclosed as a size, never a roster. The trainer's own notes are included by default (an assessment about a person is that person's data); redactions are permitted for third-party mentions and declared in the file. |
| [src/data/encryptedExport.js](../src/data/encryptedExport.js) | `data` | AES-GCM-256 + PBKDF2-SHA-256 (600k) envelope so an export can be emailed. Self-describing, so any build can open it; the passphrase is generated as readable words because it has to be dictated over a phone, and never travels with the file. |
| [src/data/recordSchemas.js](../src/data/recordSchemas.js) | `data` | Declared per-collection record shapes, per live schema major (TODO §18.1/§18.4) — the first place "schema N" exists as data rather than as a side effect of a migration transform. `fieldIssues`/`isRecordValid` are the structural validator every projection is held to. Also owns `DEFAULT_READ_SCHEMA` — the schema a fresh install reads, **declared not derived** (see [readSchema.js](../src/data/readSchema.js) for the per-install override). |
| [src/data/readSchema.js](../src/data/readSchema.js) | `data` | Which live schema THIS INSTALL reads, as a trainer-owned setting: the pre-emptive boot backfill that keeps every live schema ready, and the toggle that moves between them. An upgrade is a read re-point, not a migration — the star-write fan-out keeps the schema being left current too, so switching is instant and reversible. |
| [src/data/recordProjections.js](../src/data/recordProjections.js) | `data` | The star-write projection layer (TODO §18.1): one function per collection, domain object → IndexedDB record, plus the inverse (`groupRecordsByCollection`) that reassembles a flat record list back into `stateStore.js`'s in-memory shape. `projectionIssues()` is what proves a projection is total. |
| [src/data/recordReferences.js](../src/data/recordReferences.js) | `data` | The domain's cross-collection reference graph (TODO §18.5): which fields are structural references to another collection (vs. a denormalised "soft ref" label), and `findCycle()`/`isAcyclic()` — migration replay order needs the graph to be a DAG. |
| [src/data/recordDependencies.js](../src/data/recordDependencies.js) | `data` | Which records a given RECORD depends on, as ids per collection — the record-level companion to `recordReferences.js`'s collection-level graph. Covers the dependencies that graph's `{field: collection}` shape cannot express: `session.participants` (array of ids) and `routine.exercises` (array of objects keyed by exercise id), both of which were therefore absent from it entirely. |
| [src/data/seedProvenance.js](../src/data/seedProvenance.js) | `data` | Whether a record came from the demo seed, by stamp (`seededDemo`, written at seed time) or by the committed seed id set derived from the `DEFAULT_*` exports — the id set covering databases seeded before the stamp existed. Deliberately not by id *shape*: 8-char ids were also minted by older builds for real records. |
| [src/data/demoDataRemoval.js](../src/data/demoDataRemoval.js) | `data` | Plans the removal of demo data without gutting the trainer's own work: exercises default to KEEP (a 48-movement starter catalog), and nothing a surviving record depends on is removed — computed to a fixpoint, since retaining a record retains its dependencies in turn. `brokenDependenciesAfter()` re-checks a caller-edited plan before any write. |
| [src/data/dataWipe.js](../src/data/dataWipe.js) | `data` | What a support data wipe would remove from THIS device (TODO §31): one target per schema store the device actually has — including a store from a schema this build never heard of, which is what a long-lived install accumulates and what "wipe my data" must not leave behind — plus one for everything unversioned (the meta store and this app's own browser keys). Pure: handed the store names and keys rather than reading them. Carries what the wipe CANNOT reach (downloaded backups, a Drive copy, exports already sent) as part of the plan rather than as a footnote, because the dialog it feeds is the whole security boundary — the link that opens it carries no authority to wipe anything. |
| [src/modules/common/dataWipeDialog.js](../src/modules/common/dataWipeDialog.js) | `component` | The support data-wipe (TODO §31), reached only by the `/wipe` address support sends by SMS or email. **The link carries no authority**: opening it can only ever open this dialog, which makes the dialog the entire security boundary — so it lists what it FOUND on this device (including stores from builds this one never made), states what the app cannot reach every time rather than conditionally, and erases only on a deliberate second tap. Linked from nowhere in the app, on purpose. |
| [src/data/backupFile.js](../src/data/backupFile.js) | `data` | Builds the backup payload and reads one back. Written at the newest NUMBERED schema (`BACKUP_SCHEMA`), never at the unstable runtime shape, so any build can restore it — the cost being that a preview-only field does not reach the file. Carries `exportedAt` and `buildSha` so a restore can say which build produced it. |
| [src/data/index.js](../src/data/index.js) | `data` | Barrel for seed/demo data: `exercises.js`, `clients.js`, `routines.js`, `history.js`, `planUpdates.js`, `sessions.js`. |
| [src/data/syncMerge.js](../src/data/syncMerge.js) | `data` | Google Drive sync's three-way merge (TODO §1.5/§3.3): per-record-id merge against the last-synced ancestor, no wall-clock ordering — same-record conflicts are reported, never silently guessed. |
| [src/data/driveSyncConfig.js](../src/data/driveSyncConfig.js) | `data` | The one deployment constant Drive sync needs (TODO §1.5): `GOOGLE_DRIVE_CLIENT_ID`, blank by default and a valid "not configured" state until the maintainer fills it in. |
| [src/data/driveAppData.js](../src/data/driveAppData.js) | `data` | Google Drive `appDataFolder` REST client (TODO §1.5/§3.3): find/download/create/update the app's one hidden sync file. `fetchImpl`-injectable for tests. |
| [src/data/googleApiError.js](../src/data/googleApiError.js) | `data` | The failure shape shared by every Google REST client here (TODO §1.5): `GoogleApiError` carries the HTTP status, and `isAuthFailure` answers the only question callers ask — is the GRANT gone (401, or 403 for missing scope), or was the request merely bad? Extracted when the Calendar client became the second such caller; a second copy of a security predicate is how the two drift on what counts as "reconnect". |
| [src/data/calendarFreeBusy.js](../src/data/calendarFreeBusy.js) | `data` | Google Calendar `freeBusy.query` client (TODO §1.3/§1.5): busy intervals for a batch of room calendars in one round trip. Reports an unreadable calendar SEPARATELY rather than as an empty busy list — Google answers a partial failure with HTTP 200 and a per-calendar `errors` array, and folding that into "not busy" would draw an occupied room as available. Owns `GOOGLE_CALENDAR_FREEBUSY_SCOPE`, the narrowest scope Google publishes, deliberately not yet in the grant `googleAuth.js` requests. |
| [src/data/driveSyncService.js](../src/data/driveSyncService.js) | `data` | Orchestrates one Drive sync pass (TODO §1.5/§3.3): auth → download → three-way merge → apply locally → upload → record the new ancestor. |
| [src/data/calendarInvite.js](../src/data/calendarInvite.js) | `data` | Builds an RFC 5545 `.ics` VEVENT for a PT-assigned session (TODO §1.1) — LibrePT has no backend/SMTP relay, so this is a downloadable invite file, not a sent email. Emits `ORGANIZER`, which is the property an RSVP is mailed back to (RFC 5546): without it the file is an invitation with no return address, and most calendar clients answer it with nothing. A `CN` display name is stripped rather than escaped — a quoted parameter value has no escape sequence, so a stray quote or control character would end the content line and take the file's structure with it. Carries `RRULE` for a repeating session and `RECURRENCE-ID`+`SEQUENCE` for one evening that moved, so a client's calendar holds ONE entry for "Tuesdays and Thursdays at six" and a change lands on the evening it belongs to instead of beside it. |
| [src/data/sessionEventPayload.js](../src/data/sessionEventPayload.js) | `data` | The canonical event that travels between two devices — an invite or an RSVP — and the codec that puts it in a URL (TODO §1.6). Versioned and short-keyed because the budget is real: a QR that scans phone-to-phone holds ~300 bytes and an SMS segment is 160 characters, which is also why a session summary travels and a program never does. Decoding is parsing hostile input, so it returns null rather than throwing and copies fields by NAME — an unknown key cannot ride along into whatever the handler persists. `replyToInvite` turns an invite into the answer to it, carrying ids and one word only — a reply crosses a carrier and rests in two message histories, so a title like "Post-surgery rehab" must not travel with it; the invite also carries `organizerPhone`, which is what lets the reply be a text rather than only an email (§1.6, SMS ruled in 2026-08-17). |
| [src/data/trainerIdentity.js](../src/data/trainerIdentity.js) | `data` | The trainer's own name and email, kept for the single purpose of being an invite's `ORGANIZER`. Deliberately not a profile and not a record: one install, one trainer, nothing to sync, merge or migrate — so it sits in plain `localStorage` beside the connection flag, and is NOT version-scoped, because an address has to survive every deploy. | Now three strings, not two: `phone` was added 2026-08-17 because an invite must CARRY the trainer's number for a client to be able to reply by text — the client's device knows nothing about the trainer beyond what the invite told it. Not validated, unlike the email: numbers are written a dozen ways, the app never dials it, and rejecting a real one is the expensive mistake. || [src/data/recordId.js](../src/data/recordId.js) | `data` | Record identity (TODO §18.2): UUIDv7 as fixed-width base62 — cryptographic collision resistance, and string sort order equal to creation order. |
| [src/data/sessionItemOrder.js](../src/data/sessionItemOrder.js) | `data` | Explicit session-item ordering (TODO §17.5): dense `position` stamped by every writer, program-order reads, and the density + circuit-contiguity invariants that make a scrambled or partial item list detectable at rest. |
| [src/data/sessionCache.js](../src/data/sessionCache.js) | `data` | Active session local storage cache helper. |
| [src/domain/intakeSender.js](../src/domain/intakeSender.js) | `domain` | Puts the trainer's own name and number into an intake link's FRAGMENT and reads them back on the client's phone (TODO §26.3). The page used to ask a stranger for health details while saying only "your trainer" — the shape of a phishing attempt. This proves nothing and does not pretend to: with no server and no key exchange anybody can craft a link naming anybody. What it gives the reader is something to CHECK — the name in the message, the name on the page, and the person they just spoke to. In the fragment because a `#` reaches no server, no access log and no `Referer`, and the trainer's number is personal data too; capped on read, because a crafted link must not be able to bury the page's own words. |
| [src/domain/contactChannel.js](../src/domain/contactChannel.js) | `domain` | Reads the one contact detail a trainer was just given and says whether it is an email address, a phone number, or neither (TODO §26.3). Generous about how a number is written — spaces, dashes, brackets, a `+` — because refusing "+386 41 234 567" over spacing is the app being clever at the trainer's expense; strict about the `@`, because anything looser matches a half-typed number and offers to open a mail client for it. An unfinished line answers "neither", which is what keeps the send control out of reach until there is somewhere to send. |
| [src/domain/repsAndLoad.js](../src/domain/repsAndLoad.js) | `domain` | Polymorphic reps and equipment-derived load helpers. |
| [src/domain/exerciseModality.js](../src/domain/exerciseModality.js) | `domain` | Exercise modality axis (strength/cardio/stretch/balance) and per-metric target formatting (time/distance/calories/watts/hold). |
| [src/domain/exerciseStandard.js](../src/domain/exerciseStandard.js) | `domain` | Open-standard crosswalk: maps the catalog's category/equipment onto the wger dataset by canonical name for interchangeable JSON/CSV exports (UC6 §6). |
| [src/domain/sessionItemRecord.js](../src/domain/sessionItemRecord.js) | `domain` | Immutable history program snapshot: typed items (exercise/rest + circuit grouping), shape guards, and `buildProgramSnapshot` keeping rests + skipped work. |
| [src/domain/sessionClock.js](../src/domain/sessionClock.js) | `domain` | Wall clock ↔ schedule reconciliation for a live session: the one countdown-vs-count-up decision the bar, title bar and dashboard card all share (a session started after its slot has no countdown left, so it never opens negative), plus the ±15min start-drift test and the shifted-slot proposal behind the adjust dialog. |
| [src/domain/sessionPlanFactory.js](../src/domain/sessionPlanFactory.js) | `domain` | Builds a live session's per-client plan from the two things a session can start from — a stored history/planning snapshot or a routine (TODO §24.4). Pure: the plan's SHAPE is a training rule, while deciding when to build one is orchestration. Also owns the legacy exercise-level-rest migration and the focus-index clamp. |
| [src/domain/quickSignals.js](../src/domain/quickSignals.js) | `domain` | The rules behind the deck's one-tap Too Easy / Too Hard buttons (TODO §24.4): what counts as a disposable quick tap versus something the trainer WROTE, which tags supersede each other, and the severity order behind a card's signal colour. Pure — the controller owns the mutation and the render. |
| [src/domain/sessionFocus.js](../src/domain/sessionFocus.js) | `domain` | Which plan item the trainer is looking at, as a `{ type, id }` ref that survives a URL, a cached session and a running timer (TODO §24.4). Holds BOTH directions of the round trip — item → ref and ref → index — in one module, because they were written in three places and one of them disagreed about standalone rests. |
| [src/domain/sessionHistoryRecord.js](../src/domain/sessionHistoryRecord.js) | `domain` | One live session projected into the record stored per participant (TODO §24.4) — `sessionItemRecord.js` owns the `exercises` array inside it, this owns the record around it. Also the planning-draft upsert: one open draft per client, keeping the id a deep link and the notification feed are keyed on. |
| [src/domain/circuitGrouping.js](../src/domain/circuitGrouping.js) | `domain` | The invariants that make a circuit a circuit (TODO §24.5): members contiguous in the flat plan array, one shared title/round count per circuit, and set counts + round counters tracking the series. A circuit is not a container in the data, so this is maintained rather than enforced by structure — and every way of breaking it yields a plan that still looks plausible. |
| [src/domain/notificationItems.js](../src/domain/notificationItems.js) | `domain` | What the notification feed says, derived from state (TODO §24.7). STORED items carry i18n keys so the feed re-localises on a language switch; SYNTHETIC items (unscheduled plans, unreviewed feedback) are computed fresh and never stored — they are work the trainer owes, and a stored copy would drift from `state.history`/`state.planUpdates`. Synthetic leads, because outstanding work outranks FYI — except the demo-mode notice, which leads everything, since every other item is a claim about the trainer's own gym and reading one before knowing it is sample data is reading it wrong. |
| [src/domain/programImport.js](../src/domain/programImport.js) | `domain` | Reads a programme written in an external tool into plan items (TODO §29). Forgiving on purpose, because the result lands in the session EDITOR rather than in storage: markdown fences, prose around the JSON, a bare array, numbers as strings and `"3x10"` are all understood, via a NAMED alias table rather than fuzzy matching. Parses per item, so one unreadable row does not lose the other eleven — it survives marked, carrying its raw text — and reports EVERY failure with its position, for the summary shown before the editor opens. `programTemplate()` is the downloadable working example, parsed by a test so the app cannot hand out a demonstration of how to fail. |
| [src/domain/catalogMatch.js](../src/domain/catalogMatch.js) | `domain` | Which imported movements the catalog already knows (TODO §29.1). Exact match after case/space normalisation only: a near miss is marked **custom**, never silently adopted, because adopting the nearest entry is how a catalog becomes forty spellings of "Bench Press" under forty ids. An unknown movement is kept either way — refusing it would throw away the trainer's programme over a naming difference. |
| [src/domain/walkthroughReadiness.js](../src/domain/walkthroughReadiness.js) | `domain` | Whether the guided walkthrough has anything to walk through (TODO §28.14): a session with two participants, on a plan holding a circuit — the shapes its steps need to open a group session, focus a circuit card and switch participant. Keyed on SHAPE rather than seed ids, so a trainer's own group session qualifies and a half-cleared demo does not. Offered against a store that cannot satisfy it, the guide stops on its first step in front of the person being shown the product. |
| [src/domain/overlapLanes.js](../src/domain/overlapLanes.js) | `domain` | Lays overlapping time intervals into side-by-side lanes for §1.3's occupancy grid — which column a block takes and how many it shares width with. Serves the trainer's own sessions and calendarFreeBusy.js's read-only room blocks from one algorithm. Two decisions that are invisible in code and obvious on screen: touching blocks are SEQUENTIAL (back-to-back sessions are the normal case, and `<=` would render them clashing at half width all day), and width is shared per CLUSTER rather than per day, so one morning collision does not squeeze every unrelated afternoon session. |
| [src/domain/scheduleConflicts.js](../src/domain/scheduleConflicts.js) | `domain` | Whether a slot the trainer is about to save is a clash or the ordinary case (TODO §1.6). The whole module rests on one distinction: overlapping sessions in the SAME place are the merged-clipboard feature and must never warn, while the trainer being in two PLACES at once is impossible and must. A blank location is not treated as "somewhere else" — most trainers leave the field empty, and guessing would fire the warning on the case the app supports. External busy intervals (their own Google/Microsoft calendar) arrive as plain data, so the rules are the same whichever calendar knows about the commitment. |
| [src/domain/timeRange.js](../src/domain/timeRange.js) | `domain` | A session's scheduled slot (`"09:00 - 10:00"`) read as start/end minutes, and whether two slots collide. Two rules the callers all depend on and none of them restate: an end at or before its start CROSSES MIDNIGHT (without which a 22:00-00:00 session overlaps nothing, not even itself, and its card silently fails to launch), and touching slots are SEQUENTIAL rather than clashing. Lives in `domain/` because the conflict rules below it cannot import upwards into `modules/common/utils.js`, where it used to sit. |
| [src/domain/sessionChangeNotice.js](../src/domain/sessionChangeNotice.js) | `domain` | Which session edits a client needs to hear about, and who was actually invited (TODO §1.6). **Which changes count is the whole design**: prompting on every edit teaches a trainer to dismiss the prompt, so the list is deliberately short (refined 2026-08-17) — a moved slot, a new room, a different KIND of session (leg strength becoming cardio, which changes how a client eats and what they bring), or a different invitee list (a one-to-one that becomes a group of five is a different offer). A rename, or a reshuffled plan of the same kind, asks nothing. The session's kind is DERIVED at comparison time from the modalities its routine prescribes, because a routine's contents change independently of the session. Only clients who were **invited and are still participants** are offered a resend: a hand-added attendee was never sent anything, and someone since removed should not be chased about a session they left. |
| [src/domain/inviteExpiry.js](../src/domain/inviteExpiry.js) | `domain` | When an invitation stops being answerable (TODO §1.6): the cutoff, counted back from the session start by the trainer's padding, and whether it has passed. **Derived, never stored** — nothing runs at the cutoff, so an `expired` status would only become true if the app happened to be open. The cutoff is an absolute instant and TRAVELS in the payload, because the client's device knows only what the invite told it. Exactly at the cutoff is still open, since "four hours before" reads as "up to", and refusing that second would be a coin toss on clock skew. The DEFAULT padding deliberately lives with the setting in `data/trainerIdentity.js` — `data` may not import `domain`. |
| [src/domain/demoTour.js](../src/domain/demoTour.js) | `domain` | What a scripted demo step is, and whether it succeeded (TODO §23.5). Pure: the player supplies a probe, this decides pass/fail. **Every step must carry an expectation** — `validateTour` rejects one that does not, because a step that cannot fail is a recording again, and a tour of such steps would keep "succeeding" against a broken app. |
| [src/domain/demoStory.js](../src/domain/demoStory.js) | `domain` | Which steps a `?demo=story` link asks for, and whether the story is well-formed (TODO §35). Pure. A chapter IS a tour, so the per-step rules stay in `demoTour.js` and only the two rules that need more than one chapter live here: every chapter is named, and no step id is reused, since results are reported by id. **An unknown chapter plays the whole story rather than nothing** — these links are pasted into chat apps and typed by hand, and a player handed an empty step list looks exactly like an app that failed to boot. Resolves what a step TYPES into the viewer's language (`enterKey` → `enter`, `hasValueKey` → `hasValue`), because what a person enters in a demo is content and not a selector — Ana wrote up her shoulder in English on a Slovenian phone until 2026-08-30 (§38.19). Each step is handed its **place in the whole story** (`storyPosition`), worked out before any chapter is filtered out — the story is played by two separate boots, one per phone, and a place in the script is the only number they can agree on without sharing state (§38.9). |
| [src/domain/gymNotes.js](../src/domain/gymNotes.js) | `domain` | What the gym already said about one client, ordered for the plan on screen (TODO §35.3c/d). Pure. **In-plan first, then newest**: ordering by date alone buries the deadlift note under yesterday's unrelated signal, which is the same as not showing it — the claim being made is that a note about a movement comes back when that movement is programmed again. Resolved notes stay gone, because asking the same question twice teaches a trainer to stop reading the panel. Also owns the line a note KEPT on the client record becomes — appended to the notes the trainer writes by hand, since that is the durable text every future plan is written against. |
| [src/modules/rsvp/rsvpView.js](../src/modules/rsvp/rsvpView.js) | `rsvp` | The page a client lands on from an invite, where they answer it (TODO §1.6's confirm link). **A page rather than two links in the invite body**: an `sms:` URI inside an SMS is not linkified by most messaging apps, so the SMS leg — ruled in 2026-08-17 — would have had no working confirm route; both channels can carry a plain `https` link, so the reply URI is built here at tap time. Client-facing and therefore stateless, the same rule as `/intake`. The reply message pairs a human sentence for the trainer's inbox with a deep link whose payload names nobody. A channel is offered only after an answer is chosen, and SMS only when the invite carried the trainer's phone. |
| [src/modules/rsvp/rsvp.css](../src/modules/rsvp/rsvp.css) | `styles` | The reply page: one screen, three full-width answers, and the chosen one stays visibly chosen — the send button is a second step, and a client who cannot see what they picked will not trust what they are about to send. |
| [src/modules/intake/intakeView.js](../src/modules/intake/intakeView.js) | `intake` | The page a prospective client fills in on their own phone (TODO §1.7/§26) — **the only surface in LibrePT whose user is not the trainer**, which is where everything about it comes from: stateless (nothing is written on the client's device that outlives the tab — what they type is held in `sessionStorage` so a reload does not cost them the form, §38.12), the client picks the language their consent is recorded in, and health detail is offered rather than demanded with the copy saying where the answer goes. Consent is stamped with what they were actually shown: today's date on THEIR calendar, the wording version live at that moment, and the language they read. Static markup with values read from inputs, so stranger-supplied text never reaches an HTML sink. |
| [src/modules/intake/intakeRoute.js](../src/modules/intake/intakeRoute.js) | `intake` | The two pure questions app.js asks before choosing a boot: is this `/intake` (matched on the PATH, since it is printed on a leaflet and put behind a wall QR), and what language does this visitor read — the trainer's link may name it, otherwise the client's own device is a far better guess than the app's default. Deliberately outside the router, which is part of the trainer's app. |
| [src/modules/intake/intake.css](../src/modules/intake/intake.css) | `styles` | The intake page: one column, no header chrome, no bottom nav. Reuses index.css's form controls rather than restyling them — a client filling this in and a trainer editing a record should be typing into the same-looking field. The consent row is the whole sentence, so the tick target is not a 16px box. |
| [src/modules/intake/signupDelivery.js](../src/modules/intake/signupDelivery.js) | `intake` | How a filled-in introduction leaves the client's phone (TODO §1.7, transport ruled 2026-08-17): `navigator.share({ files })` first, because it is the only one-tap route and puts the attachment straight into the client's own mail or messaging app — `mailto:` cannot attach anything. **The download fallback is permanent, not temporary** (`canShare({files})` is false on every desktop and on iOS below 15), so it is a first-class button rather than an error path. A cancelled share is a decision, not a failure; a failed one is never reported as sent. Platform calls are injected, so no test touches `navigator`. |
| [src/modules/demo/demoPace.js](../src/modules/demo/demoPace.js) | `demo` | Turns "does this viewer want reduced motion?" into the four waits a scripted step takes. Every full-motion pause exists for an eye — find the control, watch the hand reach it, read the caption — and none of it applies to someone whose system asks for less motion, whose hand does not glide at all (demoTour.css drops the transition). Reduced motion is therefore ZERO, not merely smaller, which is safe only because a step waits on its own expectation (`outcomeBudgetMs`) instead of using its pause as a correctness device. Pure — no DOM, no timers. **The tap ring's duration is declared here too**, because the player waits a whole ring out before it clicks: the ring and the wait are one decision, and split between a module and a stylesheet they drifted until the mark was landing on the view the tap had already opened (§38.7). |
| [src/modules/demo/demoTourPlayer.js](../src/modules/demo/demoTourPlayer.js) | `demo` | Runs a tour against the real app: resolve the control, scroll it into view, move the pointer, dispatch a genuine click, then check the step's expectation. Stops at the first failure so neither a viewer nor the e2e suite sees later steps pass on top of a broken one. Scrolling lives here because the deck is its own scroll container — something Playwright cannot reach into from outside, which is one reason the tour is app code rather than a test fixture. `targetText` picks a control by its words, since position in a seeded list is not a property a demo may depend on. `performStep` is exported so the guided walkthrough (§9.5) taps by exactly this route rather than keeping its own copy of what a step means. A step can **type** and **pick from a list**, not only tap (`enter` / `choose`), firing the events a real keystroke and a real selection fire — half of what this app receives is entered rather than pressed, and a demo that can only tap has to skip exactly those screens. **A control behind an open modal counts as neither visible nor tappable**: `showModal()` makes the rest of the page inert, so a step graded against a painted-but-unreachable button finished before it happened. `replay` overrides the idempotent skip when the caller has just rebuilt the ground under the step, which otherwise leaves whatever the rebuild re-opened standing. |
| [src/modules/demo/demoHand.js](../src/modules/demo/demoHand.js) | `demo` | The animated pointer showing where the tour is tapping — built with `createElement`, never `innerHTML`, so there is no escaping question to get wrong. Optional: a tour runs and asserts correctly with no hand, which is how CI runs it. It draws the rings a tap sends out and stamps demoPace's timing onto them for the stylesheet, so the mark, the player's wait before the click and the cleanup timer all move together. |
| [src/modules/demo/gymFloorTour.js](../src/modules/demo/gymFloorTour.js) | `demo` | The script itself: open the group session, focus a circuit, signal Too Easy, switch participant. Data, not logic — adding a step is a data change. Every selector is one the e2e suite already relies on, so it cannot break alone and quietly. |
| [src/modules/demo/storySignupFile.js](../src/modules/demo/storySignupFile.js) | `demo` | The file Ana sends, in the exact shape the intake page writes, so the trainer's chapter can open it (TODO §35). This step was called unscriptable — a file picker cannot be driven by a page — and the alternative was a "hand me a submission" hook inside the shipped app, which [signupReviewDialog.js](../src/modules/clients/signupReviewDialog.js) refuses on purpose. The seam moved instead: `demoTourPlayer`'s `attach` action puts a real `File` on the real input and fires the same `change` the picker fires, so the app reads, refuses and matches exactly as it does for a file a person chose. |
| [src/modules/demo/storyTour.js](../src/modules/demo/storyTour.js) | `demo` | The long demo's script (TODO §35): the chaptered scenario that follows three friends from a leaflet to their second session — what the four-tap wedge cannot show. Data, like `gymFloorTour.js`. The gym chapter **reuses the wedge's steps** rather than restating its selectors, so a change to the clipboard breaks both loudly instead of leaving one copy quietly wrong. Steps needing unbuilt product are absent rather than mocked. **Guided, not autoplayed** (2026-08-22): four taps can be watched, four minutes cannot, so the story runs on the walkthrough's panel and the viewer performs each step or asks to be shown it. A card is **folded into the step it introduces** so nobody taps Continue on a paragraph — and the fold carries the card's ROUTE with its words, since a chapter's opening card is where the story names the screen its chapter happens on (§38.13). |
| [src/modules/demo/demoNarratorCard.js](../src/modules/demo/demoNarratorCard.js) | `demo` | Every card the demo shows, as one class hierarchy: `DemoNarratorCard` owns the shell, the three slots and the reading behaviour, and a subclass declares only what makes its kind different — a chapter's narrating voice, a message set as the quotation it is, paper's texture, and the guide's own "you have wandered off" card, which takes its words from the guide because the step's instruction names a control no longer on screen (§38.10). A kind with no class draws nothing rather than an unstyled box, and a unit test refuses any script naming one. **The card is dismissed by the step's own tap**, which is what lets a narrated step carry a real expectation instead of being a pause the player hopes was long enough. `createElement` only, so no escaping question and no CSP exposure. |
| [src/modules/demo/demoNarratorCard.css](../src/modules/demo/demoNarratorCard.css) | `styles` | One card, four kinds: the shared shell and slots, then only what each kind adds — paper's texture, a message's tint and quotation, the guide's own accent. Over an app that keeps its own appearance. The **paper** kind is a texture, not a drawn form: everything else on screen is the live app, so a drawn form would read as a real screen and send a viewer looking for it. A card that tints itself owns its text colours, because the muted tokens sit near the AA bar on the light palettes and a tint eats the margin. |
| [src/domain/walkthrough.js](../src/domain/walkthrough.js) | `domain` | Where a guided walkthrough is and which of its controls are offered (TODO §9.5). Pure. Two rules a trainer feels: **a step cannot be skipped**, because the next step's control does not exist until the current tap creates it, and **Back re-explains without undoing** — an inverse for every step would be an undo stack for a demonstration, so a step revisited stays done. Advancing is the overlay's call, not this module's: a step DONE in front of the viewer carries the card on (§38.5), one that arrived already satisfied waits for Next. Counts by a `TourNumbering` / `StoryNumbering` pair rather than a condition at the point of use: what the viewer READS is their place in the story, what the buttons OBEY is this run ending (§38.9). |
| [src/modules/demo/walkthroughOverlay.js](../src/modules/demo/walkthroughOverlay.js) | `demo` | The walkthrough's panel and spotlight over the real app. The app stays fully tappable underneath, and a **poll** watches for the step's own expectation becoming true — the same evidence the e2e suite accepts, and it does not care who caused it, so the trainer doing the step themselves is what advances the guide. "Show me" reuses `demoTourPlayer.performStep`, so a shown tap and a demo tap cannot drift. The panel moves to the top of the screen when it would cover the control it is pointing at. Runs the long story too, via an `onStep` hook the narration hangs its cards off. **The card follows the app**: a step completed in front of the viewer advances it, whoever acted — except one that arrived already satisfied (a narrated card), which would race the story two steps at a time. **Wandering off is not a fault**: when the step's control is not on this screen at all, the card says so and offers Back to the demo / Stop the demo rather than repairing under the trainer's hands. A step's GROUND includes what the step before it left on screen, so walking Back restores the last taps too — bounded to three, because a chapter the story has long left cannot be rebuilt and trying froze the guide. **Moves itself into an open `<dialog>`**: a modal makes the rest of the page inert, so a panel left in the body stops responding exactly when the story reaches the app's own note dialog — and while it is in there it LIFTS the dialog's clipping so the card can still draw at the bottom of the screen — a glass dialog is the containing block for `position: fixed` children and clips what hangs off its edges, which is what trapped the card inside the modal. A dialog that needs its own scrolling keeps its clipping and the card docks inside its visible box instead. A top-layer popover is not an escape: measured, still not clickable while a modal is open. A step is READY when its `requires` hold, its control resolves AND nothing is drawn on top of that control (hit-tested at its centre — a rect cannot see a menu over it). What the app left open is cleared first, cheapest repair before any replay: a menu through the control that opened it, a modal through its own ✕ — replaying forward is no way out of a state that makes the whole app inert, and several steps exist to open the very menu that was in the way. **One rule for advancing** (§38.18): a step completed while the trainer is watching carries the card on, whether their own thumb did it or they asked to be shown — with the last step waiting, because ending is a decision, and a step walked back to re-explained rather than skipped. **The corner PARKS the guide rather than ending it** (§38.16): everything but the head goes, leaving a bar with the step number that the trainer taps to come back — and the guide keeps polling while parked, so a step done by hand meanwhile is noticed. Ending the demo is offered only from that bar. **The panel gives up height** when a control sits where neither end of the screen clears it and the page cannot scroll (§38.15). |
| [src/modules/demo/walkthrough.css](../src/modules/demo/walkthrough.css) | `styles` | The panel and the ring around the step's control — drawn outside the control's box so it never obscures the label a trainer is being asked to recognise, and with no backdrop at all, since a scrim would block the tap the walkthrough is asking for. **Bounded to 45vh and laid out as a column**: the panel gets out of the way by docking at the other end of the screen, which needs an end to be free — a chapter's card had grown it to 63% of an iPhone SE, where neither end is (§38.15). The prose scrolls inside; the step number, the instruction and the buttons keep their own height. |
| [src/modules/demo/demoTour.css](../src/modules/demo/demoTour.css) | `styles` | The pointer only. The tour drives the app's own real controls, which keep their own appearance — a demo that restyled what it was demonstrating would be showing a different app. Honours `prefers-reduced-motion` by dropping the travel and pulse while still running the demonstration. |
| [src/domain/sessionFilters.js](../src/domain/sessionFilters.js) | `domain` | What the board is filtered to, and what one tap on a day does to it (TODO §45.6). The date model is `{ from, to }` and nothing else — a single day is `from === to`. The click rule follows documented practice (eBay, Syncfusion, airbnb/react-dates): first tap starts, second ends, and a tap once a range exists starts over. Two models we argued for are deliberately absent — alternating taps and "the nearer end moves" — because both put the next tap's meaning somewhere invisible. |
| [src/domain/sessionRecord.js](../src/domain/sessionRecord.js) | `domain` | The session form's output (TODO §24.7): the dashboard row a REAL session becomes, the slot-less meta a PLANNING session becomes instead, the day bucket, the time label, and the newly-assigned diff behind invites. The upsert MERGES, so editing a title cannot drop the `completed`/`duration` fields finishing a session stamped on it. |
| [src/domain/sessionSeries.js](../src/domain/sessionSeries.js) | `domain` | A session that repeats, and the evenings it stands for (TODO §35.3a). Pure. **A rule plus exceptions, never fifty copies**: writing "Tuesdays and Thursdays at six" out as rows would make every later edit a fifty-row migration and moving one evening indistinguishable from re-timing the lot — so occurrences are derived, and only an evening something happened to becomes a `sessions` row that speaks for it. That row is addressed by the series and the ORIGINAL date, which is what makes a second invitation a change to the same evening rather than a new one. Weekdays are JavaScript's own numbering (0 = Sunday); a second convention would be one conversion away from a missed session. |
| [src/domain/planDuration.js](../src/domain/planDuration.js) | `domain` | How long a programme takes against the slot it must fit (TODO §35.3b). Pure, and an ESTIMATE that says so: a working set is one constant (45s), because per-movement durations would mean inventing a number for all 48 seeded movements and every custom one a trainer adds — one honestly approximate number steps a hundred that look precise. Timed and held work is counted in its own seconds; rests count, since they are what a slot is actually spent on; a circuit costs its rounds. A session with no slot is never judged. |
| [src/domain/participantBinding.js](../src/domain/participantBinding.js) | `domain` | Several participants training ONE programme (TODO §8.1). Pure. A binding is a **shared plan object, not a copy**: bound members hold the same `clientState`, so every existing write — a set logged, a round completed, an exercise swapped — lands for the whole group without a single write site knowing bindings exist, where a mirror-on-write would have been a side effect at a dozen seams. What each person THOUGHT stays theirs, because feedback lives on the session and not in the plan. The list is stored separately from the plans it joins, since object identity does not survive the JSON cache. |
| [src/domain/planCopy.js](../src/domain/planCopy.js) | `domain` | Giving another participant tonight's plan (TODO §8.8) — the walk-in who joins a session already underway. **The prescription travels; the performance does not**: movements, targets, rests and circuits are what the trainer authored, while logged sets belong to whoever did them, and a copy carrying them would write a stranger's numbers into this person's history. Every item gets a fresh id, circuits remapped together, which is precisely what makes this a COPY rather than a binding (§8.1): a copy diverges the moment either plan is edited, a binding never does. |
| [src/i18n/index.js](../src/i18n/index.js) | `i18n` | Translation registry: one flat key→string map per locale (`en.js`, `sl.js`). Key parity enforced by unit tests. |
| [src/modules/sessionList/sessionsView.js](../src/modules/sessionList/sessionsView.js) | `view` | Modular view renderer for the Sessions dashboard: merges/sorts all sessions and groups them into the continuous timeline's per-day sections; owns its `<section id="view-clients">` shell markup. |
| [src/modules/sessionList/sessionsView.css](../src/modules/sessionList/sessionsView.css) | `styles` | Session booking cards, the sessions title bar/date-picker, the continuous timeline, and the floating "Create Session" button. |
| [src/modules/sessionList/sessionCard.js](../src/modules/sessionList/sessionCard.js) | `component` | Dashboard session-booking card that launches the clipboard on tap. |
| [src/modules/sessionList/sessionFilterBar.js](../src/modules/sessionList/sessionFilterBar.js) | `component` | The board's filter chips and range calendar (TODO §45.6); owns `#sessions-filter-bar`. Chips rather than a modal, because a modal hides what is switched on and a trainer then sees a short board without seeing why. Client and location are NATIVE selects wearing `.chip` — the phone's own list beats any popover we would write. The calendar is ours only because `<input type="date">` cannot show a range, and it replaced the old jump-to-date button rather than joining it. |
| [src/modules/sessionList/sessionFilterBar.css](../src/modules/sessionList/sessionFilterBar.css) | `styles` | The filter row's month grid, and the little that makes a `<select>` sit inside a chip. Ends drawn strongest, days between them a wash — the documented convention, and what makes "tap an end" a thing a person can aim at. |
| [src/modules/sessionList/sessionTimeline.js](../src/modules/sessionList/sessionTimeline.js) | `component` | Continuous, time-ordered dashboard timeline: scrollspy focus tracking, sticky-header offset sync (ResizeObserver), Today/date-jump navigation (renders its own `#sessions-date-picker` markup). |
| [src/modules/clipboard/activeSessionBoard.js](../src/modules/clipboard/activeSessionBoard.js) | `component` | Everything the active-session clipboard PAINTS (TODO §24.3): client tabs, injury banner, focus panel, the title bar's edit-mode chrome, Start/Complete visibility, and the deck-or-editor body. Extracted from `activeSessionController.js`, which orchestrates rather than renders; wired purely by injection so the board stays independently mountable. |
| [src/modules/clipboard/editModeState.js](../src/modules/clipboard/editModeState.js) | `component` | Whether the clipboard is in inline plan-edit mode and which row is being worked on (TODO §24.3). Side-effect free — the three variables have different lifetimes (a flag, a one-shot call-out, a URL-carried row id) and conflating them is what this module prevents. |
| [src/modules/clipboard/clipboardEditor.js](../src/modules/clipboard/clipboardEditor.js) | `component` | Interactive active session plan/clipboard structure editor. |
| [src/modules/clipboard/activeSessionOverlayView.js](../src/modules/clipboard/activeSessionOverlayView.js) | `component` | Markup-only: the active-session overlay shell, the add-exercise dialog, and the catalog-picker dialog. No behavior — `activeSessionController.js` calls these at boot, then drives everything. |
| [src/modules/clipboard/activeSessionOverlay.css](../src/modules/clipboard/activeSessionOverlay.css) | `styles` | The fullscreen active-session overlay shell's own CSS — title bar, timer block, footer, overflow menu. Styles the overlay chrome; `activeSessionOverlayView.js` renders the markup into `#active-session-overlay`, `activeSessionController.js` drives it. |
| [src/modules/clipboard/clipboardEditor.css](../src/modules/clipboard/clipboardEditor.css) | `styles` | The inline editor's own CSS (TODO §14.5/§18.10) — row/circuit/rest editing, reorder control, insert bar. Loaded after index.css, whose foundation it inherits. |
| [src/modules/clipboard/deckCard.js](../src/modules/clipboard/deckCard.js) | `component` | Base class for one deck card (mirrors `Route`/`route.js`): Template Method skeleton — `renderCard` draws the card's ONE design, `addFocusElements` ADDS to it when the card is in focus, then wire. Two states, not three, since §42.14 removed expand-all. `ExerciseDeckCard`/`CircuitDeckCard`/`RestDeckCard`/`PastDeckCard` supply the hooks. **One design, never a second template swapped in** (TODO §42.3): each type used to carry a collapsed and a focused template that said the same numbers differently and drifted apart. It also makes the safety rule structural — a card that is not in focus carries nothing to tap because no control was ever drawn, where a `stripControls` pass over the focused markup had already missed a button once and a number field once. |
| [src/modules/clipboard/exerciseDeckOfCards.js](../src/modules/clipboard/exerciseDeckOfCards.js) | `component` | Active-session exercise stack deck renderer — builds deck items, constructs the right `DeckCard` subclass per item, calls `.render()` uniformly. |
| [src/modules/clipboard/exerciseDeckOfCards.css](../src/modules/clipboard/exerciseDeckOfCards.css) | `styles` | Card-stack mechanics and the deck-card chrome SHARED by every card type (compact head row/timer/counter/status/name) — component-exclusive styling lives in that component's own file. |
| [src/modules/clipboard/exerciseCard.js](../src/modules/clipboard/exerciseCard.js) | `component` | `ExerciseDeckCard` — standalone exercise card in the clipboard deck: number, name, target and status on one row, with the ⏱ and the Too Easy / Too Hard / Feedback row added when it is in focus. |
| [src/modules/clipboard/exerciseCard.css](../src/modules/clipboard/exerciseCard.css) | `styles` | exerciseCard.js's exclusive styling: the Too Easy / Too Hard / Notes action row the focused card adds. The stat tiles went with §42.3 — they said the head row's own numbers a second time. Shared deck-card chrome lives in exerciseDeckOfCards.css. |
| [src/modules/clipboard/circuitCard.js](../src/modules/clipboard/circuitCard.js) | `component` | `CircuitDeckCard` — circuit/giant-set grouped block card: one line per movement (and per rest) in every state, with a feedback trio per movement, a play control per rest, the failure field and the round button added when it is in focus. |
| [src/modules/clipboard/restDeckCard.js](../src/modules/clipboard/restDeckCard.js) | `component` | `RestDeckCard` — a standalone rest, first-class and focusable like any other plan item; its Start button is ADDED only in focus, which is what fixes the collapsed-card-starts-its-timer bug. |
| [src/modules/clipboard/pastDeckCard.js](../src/modules/clipboard/pastDeckCard.js) | `component` | `PastDeckCard` — the client's most recent past session as a tappable reference card; its "focus" is `expandedPastId`, not `activeExerciseIndex`. |
| [src/modules/clipboard/circuitCard.css](../src/modules/clipboard/circuitCard.css) | `styles` | circuitCard.js's exclusive styling: round badge, per-movement rows, feedback trio, rest rows, failure field, complete-round button — with the in-focus card growing the same rows rather than styling a second set. |
| [src/modules/clipboard/exerciseAndRestTimer.js](../src/modules/clipboard/exerciseAndRestTimer.js) | `component` | Session exercise and rest countdown timer stack. |
| [src/modules/clipboard/exerciseAndRestTimer.css](../src/modules/clipboard/exerciseAndRestTimer.css) | `styles` | The floating per-client timer stack's own styling: overtime/stopped/flash states. |
| [src/modules/session/sessionBar.js](../src/modules/session/sessionBar.js) | `component` | The live-clipboard bar (`#clipboard-bar`) in the notification handle bar: names the clipboard — every merged session's title, never "the session" — carries its countdown/overtime, and is one tap back into it. Active state only; the old idle "next session" state was dropped as planning information that did not earn permanent space. |
| [src/modules/session/sessionTitleBar.js](../src/modules/session/sessionTitleBar.js) | `component` | Active-session overlay title line and countdown. |
| [src/modules/session/editSessionView.js](../src/modules/session/editSessionView.js) | `view` | Modular view renderer for Edit Session & Setup view; owns the `#view-workout-setup` shell and `#dialog-workout-setup`'s markup. |
| [src/modules/session/sessionRepeatControls.js](../src/modules/session/sessionRepeatControls.js) | `session` | The "repeats every week" half of the session form (TODO §35.3a): the weekday toggles, the reveal, and reading the form back as a rule. **The session's own day is ticked the moment the box is** — a trainer who says "this repeats" means "this, again", and a rule with no day silently produces nothing. Weekday names come from the browser's calendar in the app's language, so seven translated day names cannot drift from the dates beside them. |
| [src/modules/session/editSessionView.css](../src/modules/session/editSessionView.css) | `styles` | The compact workout-setup dialog: participant picker, checklists. |
| [src/modules/session/editSessionControl.js](../src/modules/session/editSessionControl.js) | `component` | Pre-session edit/setup control modal dialog. |
| [src/modules/session/sessionInviteDialog.js](../src/modules/session/sessionInviteDialog.js) | `component` | "Send calendar invites" dialog (TODO §1.1): offers each newly PT-assigned participant a downloadable `.ics` + prefilled mailto compose; owns `#dialog-session-invite`'s markup. | Since 2026-08-17 every invite also carries a **reply link** (§1.6's confirm link): an `.ics` collects an acceptance only from a calendar client that speaks iMIP, which is not what a gym client has, so the client answers on LibrePT's own page instead. A client with a phone number also gets a **Text it** button beside the email one — a text cannot carry the `.ics`, so email keeps the calendar file and the text carries the link. Rows are rebuilt on organizer-field input, because an `<a href>` is resolved by the browser rather than by a handler that could read those fields later. || [src/modules/session/sessionInviteDialog.css](../src/modules/session/sessionInviteDialog.css) | `styles` | The invite dialog's per-participant row and send-button states. |
| [src/modules/session/sessionStartTimeDialog.js](../src/modules/session/sessionStartTimeDialog.js) | `component` | "Session started off schedule" dialog: raised (non-blocking, after the session is already running) when Start lands more than ±15 minutes from the scheduled slot, prefilled with the slot shifted onto the clock; owns `#dialog-session-start-time`'s markup. |
| [src/modules/plans/plansView.js](../src/modules/plans/plansView.js) | `view` | Modular view renderer for Plans (formerly Routines) catalog and template editor; owns `#view-routines` and `#dialog-routine`'s markup. |
| [src/modules/plans/plansView.css](../src/modules/plans/plansView.css) | `styles` | Routine template cards + the routine builder dialog's exercise list rows. |
| [src/modules/plans/planAdjustments.js](../src/modules/plans/planAdjustments.js) | `component` | Pending Plan Adjustments deck & interactive Apply wizard; owns `#view-adjustments` and `#dialog-apply-adjustment`'s markup. |
| [src/modules/plans/programImportDialog.js](../src/modules/plans/programImportDialog.js) | `plans` | The surface for a programme written elsewhere (TODO §29, [UC9](../use_cases/uc9_program_import.md)): four optional inputs — who, which session, paste, file — a template that fills the box with a working example, and a copyable prompt for whichever assistant the trainer already uses. **Every parsing failure is shown before the editor opens**, all of them, each with its position and its own text. It writes nothing: the parsed plan opens in the ordinary editor, whose save is the app's ordinary save. |
| [src/modules/clients/clientsView.js](../src/modules/clients/clientsView.js) | `view` | Modular view renderer for Client Directory & Client profile views; owns `#view-client-directory`/`#view-client-detail`'s markup. |
| [src/modules/clients/clientsView.css](../src/modules/clients/clientsView.css) | `styles` | Client directory cards + client detail layout (profile, avatar, weight-history chart). |
| [src/modules/clients/clientsDirectory.js](../src/modules/clients/clientsDirectory.js) | `component` | Client Directory grid component. |
| [src/modules/clients/clientDataRights.js](../src/modules/clients/clientDataRights.js) | `component` | The two data-subject-request dialogs — export and erase — owning `#dialog-client-export` and `#dialog-client-erase`. Both name their target with the disambiguator rather than the name alone, because both are unrecoverable if aimed at the wrong one of two same-named clients; the erasure needs a typed confirmation and ends in a receipt listing what only the trainer can finish. |
| [src/modules/clients/clientConsentSection.js](../src/modules/clients/clientConsentSection.js) | `component` | The GDPR consent block of the Add/Edit Client dialog: signed checkbox, the date on the signed paper, the form version stamp, email/SMS delivery of the consent letter, and the "you keep the signed form" dialog; owns `#dialog-consent-info`'s markup. |
| [src/modules/exercises/exercisesView.js](../src/modules/exercises/exercisesView.js) | `view` | Modular view renderer for Exercise taxonomy catalog view; owns `#view-exercises`/`#dialog-exercise`'s markup. |
| [src/modules/exercises/exercisesView.css](../src/modules/exercises/exercisesView.css) | `styles` | Exercise library card items (muscle/taxonomy badges, instructions). |
| [src/modules/exercises/exercisePicker.js](../src/modules/exercises/exercisePicker.js) | `component` | Reusable exercise picker with taxonomy filter chips. |
| [src/modules/exercises/exercisePicker.css](../src/modules/exercises/exercisePicker.css) | `styles` | The reusable filtered exercise picker (routine builder + gym-floor swap + catalog picker). |
| [src/modules/history/historyView.js](../src/modules/history/historyView.js) | `view` | Modular view renderer for workout history logs; owns `#view-history`'s markup. |
| [src/modules/history/historyView.css](../src/modules/history/historyView.css) | `styles` | History card items, the feedback icon/tooltip, and structured history rows. |
| [src/modules/common/utils.js](../src/modules/common/utils.js) | `helper` | Shared formatting, date conversion, and string helper functions. |
| [src/modules/common/dom.js](../src/modules/common/dom.js) | `helper` | DOM helper utilities and modal helpers. |
| [src/modules/common/formDraft.js](../src/modules/common/formDraft.js) | `helper` | What a half-filled form remembers across a reload (TODO §38.12): values held in **sessionStorage**, put back when the same form comes up again, dropped on submit or on an explicit cancel. The key is a FUNCTION, because one form serves several subjects — the client dialog is "add a client" one moment and "edit Jane" the next. `sessionStorage`, never `localStorage`, is the product decision: it survives the reload that was losing people's work and dies with the tab, which is what the client's page promises them. Consent is never restored (`data-draft="never"`) — agreement is given, not carried over — and a file input cannot be restored at all, so it is not attempted. |
| [src/modules/common/download.js](../src/modules/common/download.js) | `helper` | Blob-anchor client-side file download, shared by JSON backup export and calendar-invite `.ics` download. |
| [src/modules/common/eventTransports.js](../src/modules/common/eventTransports.js) | `helper` | How an event LEAVES the device (TODO §1.6): text message, mail compose, system share sheet, clipboard — each knowing only whether it can be used right now and how to hand over a link. A registry rather than four hand-wired buttons, because with no backend every route out is somebody's messaging app and which ones exist depends on the device, the recipient and what is on file; written as branches that check would be repeated in every surface that sends anything. Copying a link is always available, so the list is never empty. Platform calls (`navigator.share`, clipboard, opening a URL) are injected, so a transport cannot quietly acquire a DOM dependency. |
| [src/modules/common/wakeLock.js](../src/modules/common/wakeLock.js) | `helper` | Screen Wake Lock API management helper. |
| [src/modules/common/activeUsersList.js](../src/modules/common/activeUsersList.js) | `component` | Active-session participant tabs component. |
| [src/modules/common/activeUsersList.css](../src/modules/common/activeUsersList.css) | `styles` | The participant-tabs row's own styling — wraps onto multiple rows for a merged group session. |
| [src/modules/common/applicationHeader.js](../src/modules/common/applicationHeader.js) | `component` | Shared top header actions, theme/lang switchers, and sync badge; owns the `#app-header` shell's markup and `#dialog-about`/`#dialog-terms`. |
| [src/modules/common/applicationHeader.css](../src/modules/common/applicationHeader.css) | `styles` | The top app header: logo, build stamp, preview badge, sync/backup button, the ☰ overflow menu. |
| [src/modules/common/backupRestore.js](../src/modules/common/backupRestore.js) | `component` | Backup center dialog and JSON import/export handlers; owns `#dialog-backup`'s markup, including the Google Drive card driveSyncUi.js wires up. |
| [src/modules/clients/signupInbox.js](../src/modules/clients/signupInbox.js) | `clients` | The two ways a client's submission reaches the trainer without a file picker (TODO §38.22): **shared into the app** from the messaging app it arrived in (the OS POSTs it, [sw/sharedInbox.js](../src/sw/sharedInbox.js) answers because there is no server to, and redirects with `?open=signup`), and **opened as a file** by tapping the `.json`, which arrives through `launchQueue`. Both end in the review dialog, so the trust boundary is untouched — a human still reads every field (§26.5). The submission is read once and dropped: the marker survives a reload, and a dialog that reopened on every refresh is one the trainer dismisses again and again. Chromium-only and installed-only, which is why the menu's picker stays. |
| [src/modules/clients/signupReviewDialog.js](../src/modules/clients/signupReviewDialog.js) | `component` | The trainer reading a submission a client sent in, and deciding what to do with it (TODO §26.5, [UC8](../use_cases/uc8_client_self_onboarding.md)). **This dialog is the feature's trust boundary**: there is no signature to verify and never will be (signing needs a key exchange, which needs a server this project does not have), so what makes a crafted file acceptable is that a human reads one record before it enters their register. Consent is shown as EVIDENCE — when, which wording, which language — not as a tick. Dedupe is an offer the trainer can decline, since two people do share a phone number, and an update never blanks a field the client left alone. Every value is rendered with `textContent`: this is the one screen in the trainer's app that displays a string a stranger wrote. |
| [src/modules/clients/intakeInvite.js](../src/modules/clients/intakeInvite.js) | `clients` | The link a trainer sends someone so they fill their OWN details in (TODO §26.3) — the case that happens far more often than the leaflet QR, and it needs no server either. Creates nothing: a person exists in the register only once they have sent their details back and the trainer has accepted them. The message says what the link is for, because a bare URL in a text is indistinguishable from phishing. Share sheet first, clipboard second, and when a browser refuses BOTH the link goes on screen to be copied by hand — a trainer standing in front of a client is never left with nothing. |
| [src/modules/clients/intakeInviteDialog.js](../src/modules/clients/intakeInviteDialog.js) | `clients` | Sending that link to one particular person (TODO §26.3 step 2): one field for both channels, because the text a trainer types already says whether it is a number or an address, and two fields on a phone are two chances to fill in the wrong one. It builds an `sms:` or `mailto:` target and the trainer's own composer opens with the invitation already written — this app never touches a carrier or a mail server. The contact is used once and never stored: a person exists in the register only after they send their own details back. The share sheet stays one tap further in, for every other channel a phone has and for a client who would rather not give a number. |
| [src/modules/common/sandboxDialogs.js](../src/modules/common/sandboxDialogs.js) | `modules/common` | The things the sandbox has to say out loud (TODO §40): the offer to rebuild one that has gone flat, the **same rebuild asked for from the menu**, and the card naming a timer that finished back in the trainer's own work while they are in here. A dialog rather than a `confirm()` for all three — they throw away everything done in the sandbox, or have to name WHOSE rest is over, and a `confirm()` can label neither of its buttons with the action it performs. **Offered and asked-for are two dialogs, not one with a flag**: the stale one explains why it interrupted and its "no" starts a cooldown, while the menu one was asked for and its "no" means nothing. Touches no storage: it asks, the caller acts. |
| [src/modules/common/demoCleanupDialog.js](../src/modules/common/demoCleanupDialog.js) | `modules/common` | The confirmation screen for clearing demo data: per-collection counts, every retained record named with the reason it survived, and an opt-out. A screen rather than a `confirm()` because the one destructive case the planner cannot rule out is per-record — a seed record the trainer edited into something of their own that nothing else references. |
| [src/modules/common/backupRestore.css](../src/modules/common/backupRestore.css) | `styles` | The Sync & Backup Center dialog's action cards. |
| [src/modules/common/driveSyncUi.js](../src/modules/common/driveSyncUi.js) | `component` | Wires the "Cloud Backup (Google Drive)" card in `#dialog-backup` to driveSyncService.js: connect/sync-now/disconnect and status rendering. |
| [src/data/googleAuth.js](../src/data/googleAuth.js) | `helper` | Google Identity Services token-client wrapper (TODO §1.5): lazily loads the GIS script only on first use, holds the access token in memory only. |
| [src/modules/common/feedbackModal.js](../src/modules/common/feedbackModal.js) | `component` | Feedback tags modal dialog and voice recorder handler; owns `#dialog-feedback`'s markup. |
| [src/modules/common/feedbackModal.css](../src/modules/common/feedbackModal.css) | `styles` | The feedback dialog's privacy-first voice-note recorder waveform animation. |
| [src/modules/common/notificationArea.js](../src/modules/common/notificationArea.js) | `component` | Toast and banner notification area handler; owns the `#notification-area` shell's markup. |
| [src/modules/common/notificationArea.css](../src/modules/common/notificationArea.css) | `styles` | The omnipresent bottom notification/status area, including the embedded active-session mini bar. |
| [src/modules/common/trainerDetailsDialog.js](../src/modules/common/trainerDetailsDialog.js) | `component` | The trainer's own name, phone and email, from the ☰ menu (TODO §45.2); owns `#dialog-trainer-details`. A form over the three values [data/trainerIdentity.js](../src/data/trainerIdentity.js) already stored — the NAME had no input anywhere in the app, while every client invitation was already signed with it. An email that is not one is refused at the field and nothing is written, the opposite of the session invite dialog's rule, where a typo must never block a send. |
| [src/modules/splash/splashScreen.js](../src/modules/splash/splashScreen.js) | `component` | Cold-start splash: dismisses `#app-splash` once the app has booted AND a minimum hold has elapsed (`max(5s, boot)`). With an empty database it instead becomes the onboarding entry point (demo data / guided walkthrough / start empty) and waits for a choice. Both offers RELOAD through `?init=demo_data_load` rather than seeding in place, and the guided one carries demo data with it because it drives the seeded sessions. `guidedDemoUrl()` is the single builder behind BOTH "show me around" offers — this one and the message feed's — and it starts the STORY (`?demo=story`, §35); until 2026-08-25 both started the four-step gym-floor tour instead. `?splash=off` disables both. |
| [src/modules/splash/splashScreen.css](../src/modules/splash/splashScreen.css) | `styles` | The splash overlay: themed background (`--bg-color`/`--bg-gradient`, so it matches whatever theme is set), staggered entrance animations, indeterminate progress sweep, the onboarding action stack, and a static frame under `prefers-reduced-motion`. |
| [src/modules/common/buildInfoDialog.js](../src/modules/common/buildInfoDialog.js) | `component` | Tappable build identity (commit, data schema, build time) as a copyable dialog — the phone-reachable replacement for a hover tooltip; owns `#dialog-build-info`'s markup. |
| [src/modules/common/buildInfoDialog.css](../src/modules/common/buildInfoDialog.css) | `styles` | The build-info dialog's fact rows. |
| [src/modules/common/encryptedFileReader.js](../src/modules/common/encryptedFileReader.js) | `component` | The CLIENT's half of a data export: opens an encrypted attachment in the browser, on their own device, with the passphrase their trainer sent separately. In the header menu rather than on a client record, because the person opening it has no client record — and in the app rather than as a self-decrypting HTML attachment, which mail gateways strip. Owns `#dialog-open-encrypted`. |
| [src/modules/common/consentForm.js](../src/modules/common/consentForm.js) | `component` | Delivery of the GDPR consent letter: which language it goes out in, the version stamp, the per-locale GitHub URLs of the client documents, and the `mailto:`/`sms:` builders behind the two buttons. The letters themselves live in [src/i18n/consent/](../src/i18n/consent/index.js). |
| [src/modules/themes/](../src/modules/themes/) | `styles` | Theme-specific CSS stylesheets (`daylight.css`, `midnight.css`, `red.css`, `blossom.css`, `nebula.css`). |
| [src/fonts/](../src/fonts/) | `assets` | Locally-vendored variable webfonts (DM Sans, Outfit, JetBrains Mono; latin + latin-ext) + `fonts.css`, and Font Awesome 6.4.0 + `fontawesome.css` since 2026-08-05 — so the offline-first PWA has no `fonts.googleapis.com`/`fonts.gstatic.com`/`cdnjs.cloudflare.com` dependency at all (regeneration steps in each CSS header). |
| [src/controllers/routerController.js](../src/controllers/routerController.js) | `controller` | SPA route mapping and navigation logic: base path, history writes, and the facade of operations a route may perform; owns `#view-error`'s markup. |
| [src/controllers/routes/route.js](../src/controllers/routes/route.js) | `controller` | Base `Route` class: pattern ↔ params translation (`match`/`build`), specificity, and the shared enter/exit chrome lifecycle. |
| [src/controllers/routes/routeRegistry.js](../src/controllers/routes/routeRegistry.js) | `controller` | The ordered route collection: register, resolve a path to one route by specificity, and spell a URL for a named route. |
| [src/controllers/routes/dialogRoute.js](../src/controllers/routes/dialogRoute.js) | `controller` | `DialogRoute` / `GlobalDialogRoute`: a <dialog> as an addressable state layered over a parent route's view, so Back closes it and a reload reopens it. |
| [src/controllers/routes/viewRoute.js](../src/controllers/routes/viewRoute.js) | `controller` | `ViewRoute` (a `#view-*` on screen, optionally re-rendered) and `RedirectRoute` (rewrite one URL into another, render the target in place). |
| [src/controllers/routes/sessionRoutes.js](../src/controllers/routes/sessionRoutes.js) | `controller` | Routes that resolve a record first: session timeline, live/recovered session (focus + edit modes), workout setup, client detail. |
| [src/controllers/routes/routeTable.js](../src/controllers/routes/routeTable.js) | `controller` | Every addressable state of the app as data — the route patterns, stored without the base path so they stay version-agnostic. |
| [src/controllers/appLifecycleController.js](../src/controllers/appLifecycleController.js) | `controller` | PWA runtime lifecycle: SW registration, integrity-error page, online/offline state. |
| [src/controllers/backupHealthController.js](../src/controllers/backupHealthController.js) | `controller` | Holds the two ASYNC inputs the unbacked-data warning needs (the IndexedDB backup history, the `navigator.storage` estimate) so the assessment itself stays synchronous on every write — two round-trips per edit, to answer a question that changes on the order of days, is not a cost to put on the gym floor. |
| [src/controllers/clientFormsController.js](../src/controllers/clientFormsController.js) | `controller` | Client create/edit dialog: markup + wiring; owns `#dialog-client`. |
| [src/controllers/routineFormsController.js](../src/controllers/routineFormsController.js) | `controller` | Routine create/edit dialog and its exercise-picker-backed builder list; owns `#dialog-routine`. |
| [src/controllers/exerciseFormsController.js](../src/controllers/exerciseFormsController.js) | `controller` | Custom-exercise create dialog, including the modality-driven metric selector; owns `#dialog-exercise`. |
| [src/modules/common/populateDropdownSelectors.js](../src/modules/common/populateDropdownSelectors.js) | `component` | Repopulates the routine `<select>` and session exercise `<datalist>` from current state — shared by all three form controllers and activeSessionController, so it isn't owned by any one of them. |
| [src/sw.js](../src/sw.js) | `service-worker` | Thin classic-worker entry: loads the sw/ modules via `importScripts` and wires the install/activate/fetch lifecycle events. |
| [src/sw/cacheManifest.js](../src/sw/cacheManifest.js) | `service-worker` | The offline cache's versioned identity (`CACHE_NAME`), the exact app-shell `ASSETS` set, and cache open/purge/write ops. |
| [src/sw/integrity.js](../src/sw/integrity.js) | `service-worker` | Loads the SHA-256 integrity catalog (`integrity.json`) and verifies each precached asset's hash. |
| [src/sw/precache.js](../src/sw/precache.js) | `service-worker` | The install-time verified atomic precache; fails loud (integrity error page) on an unverifiable build. |
| [src/sw/sharedInbox.js](../src/sw/sharedInbox.js) | `sw` | Answers the POST the phone's share sheet makes when a file is shared INTO LibrePT (TODO §38.22). The worker has to, because a share target is a URL the OS posts to and this app is a folder of static files — without it the manifest would send a trainer's phone at a URL that 404s. Keeps the file's text in its own cache (spared from the version purge, or a deploy would throw away an unread submission) and answers 303, since only See Other turns the POST into the GET that loads the app. |
| [src/sw/runtimeFetch.js](../src/sw/runtimeFetch.js) | `service-worker` | The runtime fetch strategy: network-first shell with offline cache fallback, cache-first for third-party assets. |
| [src/version.js](../src/version.js) | `entry` | Build/deploy stamp shown in the header so a screenshot pins a bug report to an exact build. The checked-in copy ships as `dev`; the build and the Pages deploy overwrite `dist/version.js` with the real short commit SHA. |
| [src/controllers/activeSessionController.js](../src/controllers/activeSessionController.js) | `controller` | Composition root for the live gym-floor session: wires the board with everything it paints, owns edit mode and the overlay's title-bar chrome, and re-exports the session's public surface from the modules below. |
| [src/controllers/activeSessionStore.js](../src/controllers/activeSessionStore.js) | `controller` | The one live session and the app-level deps the session code is wired with, behind accessors — the bottom of the session controller stack, so the modules split out of the controller share state without importing each other. |
| [src/controllers/activeSessionCache.js](../src/controllers/activeSessionCache.js) | `controller` | The save side of the session cache, plus the planning-draft upsert that must ride along with every save, so no caller has to remember that a planning clipboard persists differently. |
| [src/controllers/sessionFocusUrl.js](../src/controllers/sessionFocusUrl.js) | `controller` | The address bar as a view of where the trainer is standing: builds the focus (or editor-row) URL, replaces it when a render moves the focus, and moves the focus when a card is tapped. |
| [src/controllers/sessionTimers.js](../src/controllers/sessionTimers.js) | `controller` | The session's own elapsed-time interval and title-bar readout, plus starting a labelled per-client rest/exercise timer in the stacked overlay. |
| [src/controllers/sessionQuickSignals.js](../src/controllers/sessionQuickSignals.js) | `controller` | One-tap too-easy/too-hard/note marks: mutates session feedback and the plan-adjustment queue in step. The rules themselves are pure and live in `domain/quickSignals.js`. |
| [src/controllers/sessionCircuits.js](../src/controllers/sessionCircuits.js) | `controller` | Circuits as the deck sees them: groups a flat plan into circuit units for rendering, and advances a circuit through its rounds when the trainer taps it done. |
| [src/controllers/sessionPlanEditing.js](../src/controllers/sessionPlanEditing.js) | `controller` | Changing WHAT is in an open plan — adding a movement (typed or from the taxonomy), swapping a row's movement in place, emptying the plan — and the two dialogs that drive it. |
| [src/controllers/sessionScheduleAdjustment.js](../src/controllers/sessionScheduleAdjustment.js) | `controller` | "The gym ran late": offers to move a scheduled slot onto a session that started well outside it, and writes the accepted correction to every place a slot is held. |
| [src/controllers/sessionLifecycle.js](../src/controllers/sessionLifecycle.js) | `controller` | How a session begins and ends: staging a plan, tapping Start, recovering one after a reload, cancelling, deleting the slot behind it, and completing it into history. |
| [src/controllers/gestureController.js](../src/controllers/gestureController.js) | `controller` | Touch gestures: view title-bar drag/swipe-to-dismiss (70px commits the gesture). |
| [src/modules/common/renderRegistry.js](../src/modules/common/renderRegistry.js) | `component` | Structural guard for TODO §14.8: each view shell registers itself plus what must exist first, and a topological sort computes a valid render order — replacing a hand-ordered call list that silently no-op'd when a module queried an element rendered after it. |
| [src/modules/common/shareLink.js](../src/modules/common/shareLink.js) | `component` | Promo/share deep links for the demo instance: preselected UI language, colour theme, the optional `?init=demo_data_load` dataset initializer, and `?demo=` — `gym_floor` (the four-tap wedge plays itself), `walkthrough` (the trainer drives that same wedge, §9.5) or `story` (the chaptered demo, TODO §35, with `chapter` and `step`). Named values read by their own boot steps, so a link asking for one cannot start another. The app's own "show me around" offers all build `story`. |
| [src/modules/common/syncStatusGlyph.js](../src/modules/common/syncStatusGlyph.js) | `component` | Pure mapping from a Drive sync status to the header cloud's overlay glyph, state class and spoken label (TODO §3.11) — four ordered states: syncing, failed, not-connected, idle. DOM-free so the vocabulary is unit-testable; `applicationHeader.js` does the painting. |
| [src/modules/common/theme.js](../src/modules/common/theme.js) | `component` | The one theme service (TODO §24.1): resolve / apply / persist / localize, plus the `#theme-switcher` wiring. In `modules/common/` rather than `controllers/` because it orchestrates nothing — the old `controllers/themeController.js` placement was unreachable from the header, which duplicated it instead. `src/theme-boot.js` keeps its own pre-paint copy of the map on purpose. |
| [src/modules/common/driveSyncUi.css](../src/modules/common/driveSyncUi.css) | `styles` | Styles for the Google Drive sync card inside the Sync & Backup dialog. |
| [src/i18n/domMappings.js](../src/i18n/domMappings.js) | `i18n` | Static DOM-selector → translation-key map, applied on every language switch. |
| [src/i18n/en.js](../src/i18n/en.js) | `i18n` | English translation dictionary (the fallback locale). |
| [src/i18n/sl.js](../src/i18n/sl.js) | `i18n` | Slovenian translation dictionary. |
| [src/i18n/consent/index.js](../src/i18n/consent/index.js) | `i18n` | Registry of the GDPR consent letter per locale, with the endonym labels the client-facing language picker shows. Its keys must match `TRANSLATIONS` exactly — a UI language with no letter would silently send English to a client offered their own language. |
| [src/i18n/consent/en.js](../src/i18n/consent/en.js) | `i18n` | English consent letter (subject, body, SMS one-liner) — the source edition, pinned to [docs/templates/en/Client_Consent_Form.md](templates/en/Client_Consent_Form.md). |
| [src/i18n/consent/sl.js](../src/i18n/consent/sl.js) | `i18n` | Slovenian consent letter, pinned to [docs/templates/sl/Client_Consent_Form.md](templates/sl/Client_Consent_Form.md). Maintainer-translated, not legally reviewed. |
| [src/data/clients.js](../src/data/clients.js) | `data` | Seed client roster for the demo dataset. |
| [src/data/exercises.js](../src/data/exercises.js) | `data` | Seed exercise catalog — the movement taxonomy's starting corpus. |
| [src/data/routines.js](../src/data/routines.js) | `data` | Seed routine/plan templates for the demo dataset. |
| [src/data/sessions.js](../src/data/sessions.js) | `data` | Seed sessions, generated relative to "now" so the demo dashboard always has a live/upcoming day. |
| [src/data/sessionSeriesSeed.js](../src/data/sessionSeriesSeed.js) | `data` | The demo's one REPEATING session, anchored to "today" like every other seeded time, **and the evenings it has already held**. One rule, not three: the point is that a repeating slot exists and is edited in one place, and a second would only add cards to scroll past. It starts a week BACK, so the board has evenings behind it as well as ahead — "move only the next one" means nothing on a rule with no past. Those past evenings are seeded as FINISHED sessions (`DEFAULT_SERIES_PAST_SESSIONS`, folded into `sessions.js`): a derived evening can never be marked finished, so one left derived shows on the board as a session still waiting to be run, counting up — a freshly built sandbox opened on "Overdue 64h". |
| [src/data/history.js](../src/data/history.js) | `data` | Seed completed-session history, including a structured program snapshot. |
| [src/data/planUpdates.js](../src/data/planUpdates.js) | `data` | Seed pending plan adjustments (unresolved client feedback) for the adjustments deck. |
| [src/data/messages.js](../src/data/messages.js) | `data` | Seed notification/message feed the notification area renders from. |
| [src/fonts/fonts.css](../src/fonts/fonts.css) | `styles` | LibrePT-vendored variable webfonts (DM Sans, Outfit, JetBrains Mono), latin + latin-ext only, so the offline-first PWA has zero third-party font origin. |
| [src/fonts/fontawesome.css](../src/fonts/fontawesome.css) | `styles` | LibrePT-vendored Font Awesome Free 6.4.0 (woff2 only), replacing the cdnjs stylesheet — the last cross-origin asset. Upstream-minified, so it is the one file Biome is configured to skip; regenerate by re-downloading rather than editing (steps in its header). |
| [src/modules/themes/daylight.css](../src/modules/themes/daylight.css) | `styles` | Daylight theme palette (the default). |
| [src/modules/themes/midnight.css](../src/modules/themes/midnight.css) | `styles` | Midnight theme palette. |
| [src/modules/themes/red.css](../src/modules/themes/red.css) | `styles` | Red theme palette. |
| [src/modules/themes/blossom.css](../src/modules/themes/blossom.css) | `styles` | Blossom theme palette. |
| [src/modules/themes/nebula.css](../src/modules/themes/nebula.css) | `styles` | Nebula theme palette. |
